import 'package:flutter/material.dart';

class VaccineInfoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Scaffold(
        body: Center(
      child: Text("Vaccine Info Page"),
    ));
  }
}
